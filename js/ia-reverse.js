// Code goes here

 $(document).ready(function() {
  
  
  
  
  
  
  
  
 // reverses order of divs so site appears to flow backwards 
  // var reverse = $('#content')[0],
  //     divs = reverse.children,
  //     order = divs.length - 1;
      
  // for (order; order > 0; order--) {
  //   reverse.appendChild(divs[order]);
  // }
  
});


// var parent = document.getElementsByClassName('top-to-bottom')[0],
//     divs = parent.children,
//     i = divs.length - 1;

// for (; i--;) {
//     parent.appendChild(divs[i])
// }